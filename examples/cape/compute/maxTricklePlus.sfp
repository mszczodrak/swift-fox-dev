configuration disseminate { maxTest(0, 10240) 
			tricklePlus(1, 1024, 1, 6)
                        csmaca(0, 0, 10, 10, 1, 1, 1)
                        # always ON, delay_after_receive, backoff, min_backoff
                        # ack, cca, crc
			cape()
       #                 cc2420(26, 31, 1, 1)
                        # channel, power, ack, crc
			}

state test {disseminate}

start test

