configuration compute { max()
		broadcast()
		csmaca(dest_node, 0, 10, 10, 1, 1, 1)
		# always ON, delay_after_receive, backoff, min_backoff
		# ack, cca, crc
		cape()
}

state computation { compute }

start computation
