uint16_t dest_node = 73
uint16_t src_node = 0xFFFF

configuration count { counter(6, 1024, src_node, dest_node) 
			nullNet()
			#ctp(dest_node)
			macxMac()
			nullRadio()
			#cc2420x()
                        #csmaca(dest_node, 300, 10, 10, 1, 1, 1)
                        # always ON, delay_after_receive, backoff, min_backoff
                        # ack, cca, crc
                        #cc2420(26, 31, 1, 1)
                        # channel, power, ack, crc
			}

state test { count }

start test

