
configuration tester { phidget_adc(300, 0, 2, 400, 7, 3, NODE) # Send to itself 
			nullNet()
		      	#ctp(2)
                        csmaca(2, 0, 10, 10, 1, 1, 1)
                                # always ON, delay_after_receive, backoff
                                # min_backoff, ack, cca, crc
                        cc2420(26, 31, 1, 1)
                                # channel, power, ack, crc
}
start tester

