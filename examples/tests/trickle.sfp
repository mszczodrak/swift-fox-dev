uint16_t dest_node = 0xFFFF
uint16_t src_node = 3

configuration disseminate { counter(15, 1024, src_node, dest_node) 
			trickle(src_node)
                        csmaca(src_node, 0, 10, 10, 1, 1, 1)
                        # always ON, delay_after_receive, backoff, min_backoff
                        # ack, cca, crc
			cape()
#                        cc2420(26, 31, 1, 1)
                        # channel, power, ack, crc
			}

state test {disseminate}

start test

