uint16_t dest_node = 3

configuration count { 
			counter(10, 1024, 1, dest_node)
                        ctp(dest_node)
                        csmaca(dest_node, 0, 10, 10, 1, 1, 1)
                        # always ON, delay_after_receive, backoff, min_backoff
                        # ack, cca, crc
			cape() 
#                        cc2420(26, 31, 1, 1) 
#			nullRadio() 
}

state simple { count }

state notsimple { count count }

start simple

