uint8_t node_addr = 3

configuration count { 
			counter(10, 1024, NODE, 74)
                        ctp(74)
                        csmaca(74, 0, 10, 10, 1, 1, 1)
                        # always ON, delay_after_receive, backoff, min_backoff
                        # ack, cca, crc
			cape() 
#                        cc2420(26, 31, 1, 1) 
#			nullRadio() 
}

state simple { count }

state notsimple { count count }

start simple

